services:
  - type: web
    name: ders-programi-solver
    runtime: python
    rootDir: program
    plan: free
    buildCommand: pip install -r requirements-solver.txt
    startCommand: uvicorn ortools_solver:app --host 0.0.0.0 --port $PORT
